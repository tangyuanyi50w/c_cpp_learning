
	1、建立本地工作目录和远程仓库的连接是关键，这需要设置ssh密钥；
	2、本地工作目录，必须使用git init生成隐藏的.git文件，用来标识其为本地工作目录（或者通过clone命令将远程仓库下载到本地后，自带.git）；
	3、每次操作前，最好将远程的仓库pull一下。这样可以避免当远程仓库被修改时，由于二者版本不匹配而无法上传的问题（可能是.git里边的哪个
文件导致的结果）；
	4、基本原理看b站“狂神说”，具体git指令看菜鸟教程/git教程；
	5、关于从远程到本地的过程其实还不是很清楚，导致使用时容易出现各种错误，待研究。